syntax = "proto3";

package app_config;

message Connection {
    uint32 ip = 1;
    uint32 port = 2;
    string mode = 3;
};

message Config{
    uint32 config = 1;
    string name = 2;
    Connection connection  = 3;
};